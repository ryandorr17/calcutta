<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= form_for(@auction_participant) do |f| %>
      <% if @auction_participant.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@auction_participant.errors.count, "error") %> prohibited this auction_participant from being saved:</h2>

          <ul>
          <% @auction_participant.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.label :user %>
      <%= f.number_field :user_id, class: 'form-control'  %>

      <%= f.label :auction %>
      <%= f.number_field :auction_id, class: 'form-control'  %>

      <%= f.label :owner %>
      <%= f.radio_button :owner_flag, class: 'form-control'  %>



      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>

  </div>
</div>
